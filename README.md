1. compile and flash the esp32 s3 with cam. i used esphome
2. run the docker-compose.yaml - it transforms the output of the s3 into a 5fps h264 stream

(optional) use the 3d printable files to create a housing for the hardware.
